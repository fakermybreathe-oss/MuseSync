# MuseSync 今日开发总结与 VPS 后端部署指南 (2026-06-05)

本文档总结了今天对 MuseSync 项目进行问题排查、代码分析的成果，并提供了更新海外 VPS 后端服务的具体指南。

---

## 🌟 1. 今日开发进展与排查结论

### 1.1 网易云音乐可以搜索但播放失败问题排查
* **当前现状**：网易云的搜索功能运行完全正常，但点击播放歌曲时没有声音或提示加载失败。
* **已做优化与改动**：
  1. **前端 Cookie 穿透升级**：在本地获取音频的 API 请求（`/api/netease/song/:id`）由 `GET` 升级为 `POST`，以便把保存在客户端 `localStorage` 中的用户登录 Cookie 传回给后端。
  2. **后端接收与权限探测**：后端路由成功接收 Cookie 并调用 `ncm.song_url_v1`（请求 exhigh 音质）。
  3. **互补逻辑规则修缮**：废除了原先以 `fee > 0` 判定触发互补的粗糙逻辑，修改为严格判断：**仅在歌曲无音轨链接、明确为试听截断（有 `freeTrialInfo`）或实际音轨时长小于歌曲总时长 10 秒以上**时，才静默跨端调用 QQ 音乐 SVIP 互补；避免了普通 VIP 歌曲直接降级为普通音质。
* **播放失败的可能根本原因**：
  1. **VPS 节点 IP 限制**：海外 VPS 节点的公网 IP 直接抓取网易云音乐 CDN 音频流可能遇到了网易云官方的防抓取限制（API 常返回 `-460` 机器人检测错误，或音频流连接被网易云 CDN 阻断）。
  2. **Cookie 失效或未注入**：即使重新登录，若传回的 Cookie 格式不对或有缺失，网易云将限制播放，需在 VPS 后端查看详细的请求日志才能得出网易云返回的具体错误码。

### 1.2 QQ 音乐可以播放歌单但搜索失败问题排查
* **当前现状**：点击个人歌单里的歌曲可以完美播放，但是使用搜索框进行 QQ 搜索时，页面无反应或无搜索结果。
* **排查结果**：
  1. **本地测试完全连通**：我们在开发环境下对本地后端 `http://localhost:8080/api/qq/search?keyword=test` 进行了直接测试，结果显示接口**可以正常连通**，并返回了包含 `id`、`title`、`artist`、`album` 等完整字段的标准歌曲 Track 数组。
  2. **防御性修复**：先前搜索失败可能导致前端在遍历结果时引发 React 白屏崩溃。我们已经在前端 `MuseSyncPlayer.tsx` 的 `handleSearch` 中加入了防崩溃的 `Array.isArray(data)` 保底逻辑。
* **搜索失效的根本原因**：
  * **VPS 后端代码未更新**：用户当前的海外 VPS 仍然在后台运行老版本的后端程序。老版本在处理 QQ 搜索时，如果遇到 3200 端口响应格式异常，会向前端返回 `{ error: 'QQ Search failed' }` 对象，而旧前端没有数组防御，直接报错导致功能失效。

---

## 🛠️ 2. 海外 VPS 后端服务更新保姆级教程

由于本地 SSH 连接 VPS (`207.57.131.146:50520`) 必须要手动输入密码，AI 无法在无终端交互的情况下自动输入。**请您在本地的 PowerShell 或命令行终端中，手动执行以下几步来更新您的 VPS 后端**：

### 第一步：SSH 登录您的 VPS 宿主机
请打开您的 Windows 终端，执行以下命令，并输入您的 VPS SSH 密码：
```bash
ssh -p 50520 root@207.57.131.146
```

### 第二步：进入项目目录并拉取最新 GitHub 代码
成功进入 VPS 终端后，依次运行以下命令进入您的 MuseSync 部署文件夹并同步代码：
```bash
# 1. 进入您的项目目录（请根据您在 VPS 上的实际目录修改路径，以下为默认路径）
cd /root/新webapp

# 2. 从 GitHub 拉取最新的主分支代码（今日修改已被 PUSH 到了远程库）
git pull
```

### 第三步：安装依赖并重新打包构建后端
```bash
# 3. 安装依赖（以防止依赖库更新）
pnpm install

# 4. 编译打包后端（将 TypeScript 打包编译为 dist/index.js 单文件）
pnpm --filter @musesync/server run build
```

### 第四步：使用 PM2 重启后端守护进程
在宿主机中重启托管后端与 API 代理的 PM2 服务：
```bash
# 5. 重启主后端中枢服务
pm2 restart musesync-backend

# 6. 重启 QQ 音乐 API 代理服务
pm2 restart qq-music-api

# 7. 查看当前运行状态，确保两个服务都处于 online 状态
pm2 list
```

---

## 🚀 3. 下一次开发的重点任务与验证方向

1. **功能可用性验证**：
   * 在 VPS 后端成功拉取并重启后，重新测试 **QQ 音乐搜索功能**，确认不再失效并且能正常点歌。
   * 测试 **网易云播放功能**，如果仍然失败，我们将通过查看 VPS 的 PM2 后端日志（`pm2 logs musesync-backend`）定位其具体返回的报错（例如是 Netease Api 的 `-460` 错误，还是代理音频的 CORS 失败）。
2. **代理优化与伪装机制**：
   * 针对网易云的播放失败，可能需要在后端音频代理中加入更多的请求头伪装（如更复杂的 User-Agent 和特定的网易云 CDN 加密参数）。
   * 提升跨端同播的高精进度对齐效果。
