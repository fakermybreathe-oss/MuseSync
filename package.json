{
  "name": "musesync-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
